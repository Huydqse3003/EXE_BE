# Copilot Instructions

## Project Guidelines
- User prefers not to use Task<ApiResponse> wrappers for auth flow and wants direct entity response DTOs returned from API/service.
- User wants AuthService methods to return Task<ApiResponse> in the service layer, following response-wrapper style.